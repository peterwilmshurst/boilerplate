/**
 * Adder
 * @param {number} p1 Number 1
 * @param {number} p2 Number 2
 * @returns {number} Total
 */
export const adderFn = (p1, p2) => p1 + p2

/**
 * Adder using type file
 * @type {import('./types/types.js').AddFn}
 */
export const adderFn2 = (p1, p2) => p1 + p2

// const a = '2'
// const b = a.blah()
// const c = adderFn(5, 6)
// console.log('Hello world!', c)
